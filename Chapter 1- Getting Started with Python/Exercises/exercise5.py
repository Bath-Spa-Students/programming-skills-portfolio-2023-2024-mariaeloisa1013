#allow float input into variable, radius
radius = float(input("\n What's the radius of the circle? "))

#assign varibale, area to solution
area = (3.14*(radius**2))

#print result
print ("If the radius is " + str(radius) + ", then the area will be: " + str(area) + " cm\u00B2")
